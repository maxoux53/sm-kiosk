meta {
  name: nettoyage - suppression achat
  type: http
  seq: 34
}

delete {
  url: http://localhost:3001/v1/interact/purchase/{{purchaseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}

tests {
  pm.test("La suppression retourne un body vide (204)", () => {
    pm.expect(pm.response.text()).to.be.empty;
  });
}
