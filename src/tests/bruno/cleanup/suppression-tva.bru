meta {
  name: nettoyage - suppression TVA
  type: http
  seq: 37
}

delete {
  url: http://localhost:3001/v1/interact/vat/Z
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}

tests {
  pm.test("La suppression retourne un body vide (204)", () => {
    pm.expect(pm.response.text()).to.be.empty;
  });
}
