meta {
  name: event - achats de l'événement
  type: http
  seq: 30
}

get {
  url: http://localhost:3001/v1/interact/event/{{eventId}}/purchases
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 200
}

tests {
  pm.test("La réponse contient un tableau d'achats", () => {
    const json = pm.response.json();
    pm.expect(json).to.be.an("array");
  });
}
